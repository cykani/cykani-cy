---
title: Stealth Mode
description: How Cykani's stealth patches work
---

## Overview

Cykani patches Chromium at the C++ level with 26 stealth patches. This makes automation indistinguishable from real browser usage.

## Detection Tests Passed

- Cloudflare Turnstile
- reCAPTCHA v3 (0.9 score)
- FingerprintJS
- BrowserScan
- Akamai
- CreepJS

## How It Works

### C++ Patches

Unlike JavaScript polyfills, Cykani modifies the actual Chromium source:

- **Canvas** — Randomized rendering noise
- **WebGL** — Vendor/renderer string spoofing
- **Audio** — AudioContext fingerprint randomization
- **Fonts** — System font enumeration masking
- **GPU** — WebGL renderer string isolation
- **Timezone** — Per-session timezone isolation
- **User Agent** — Consistent UA across all APIs

### Fingerprint Isolation

Each session gets a unique, deterministic fingerprint based on a seed:

```javascript
const session = await strike({ fingerprint: 42 });
// Same seed = same fingerprint every time
```

### Human-Like Behavior

- Bezier curve mouse movements
- Cognitive hesitation before actions
- Idle micro-movements
- Random coffee breaks
